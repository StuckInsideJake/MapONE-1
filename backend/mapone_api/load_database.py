from mapone_api.models import Publication

def add(publication, filename):
    # TODO - add publication to database from web scraper

    # Create objects in database
    # For loop
    # Publication.objects.create(
    #     source=,
    #     link=,
    #     body=,
    #     scale=,
    #     author=,
    #     publication_info=
    # )
